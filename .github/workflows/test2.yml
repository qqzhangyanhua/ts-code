name: Github action test22222
on: [push]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - run: echo '${{github.action}}'
      - run: echo "${{github.ref}}"
      - run: echo "${{github.repository}}"
